#include "main.h"

/**
 *_isdigit - checks for a digit (0 through 9)
 *
 *@c: an integer input
 *
 * Return: 1 in case uppercase letter
 *	always 0 (Success)
 **/

int _isdigit(int c)
{
	for (c = 48; c <= 57; c++)
	{
		return (1);
	}
	return (0);
}
